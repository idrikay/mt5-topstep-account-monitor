apiVersion: v1
kind: ServiceAccount
metadata:
  name: trading-dashboard
  namespace: apps
  labels:
    app: trading-dashboard
# No automountServiceAccountToken — the application does not need to call
# the Kubernetes API, so there is no reason to expose a token inside the pod.
automountServiceAccountToken: false
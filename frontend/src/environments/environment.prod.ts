export const environment = {
  production: true,
  // Set this to your backend's public URL, e.g. the DigitalOcean LoadBalancer
  // IP or a custom domain pointing to it.
  backendUrl: 'https://api.your-domain.com',
};
 
/** Trades within ±this value (USD) are classified as breakeven. */
export const BREAKEVEN_THRESHOLD = 10;

/** Maximum number of trades kept in memory per account. */
export const MAX_TRADES_CACHED = 100;

/** How often (ms) the position duration display refreshes. */
export const DURATION_REFRESH_INTERVAL_MS = 10_000;